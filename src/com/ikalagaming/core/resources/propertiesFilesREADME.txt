Properties files need to have localizations created of them before shipping.
As of now there is only one properties file per class/bundle so that it is
easier to edit without handling translations or the en_US/en_GB which would be
pretty much identical to the default as I default to English.